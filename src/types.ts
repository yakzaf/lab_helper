export interface DTAttr {
  rows: string;
  columns: string;
  id?: string;
  className?: string;
}

export interface ChartAttr {
  className?: string;
  tableId?: string;
  plotTitle: string;
  type: string;
}
